[phases.setup]
nixPkgs = ["php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.openssl", "php82Extensions.curl", "nodejs"]

[phases.install]
cmds = []

[phases.build]
cmds = []

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t . router.php"
