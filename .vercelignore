# Legacy directories no longer in project (entries kept as safety net)
js/
css/
docs/

# DB schema — not a serverless function, exclude from function bundler
db/

# Local dev scripts — not serverless functions
scripts/

# Secrets — never deploy
.env
.env.local
.env.*.local

# Source maps
*.map

# OS junk
.DS_Store
Thumbs.db
